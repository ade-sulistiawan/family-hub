# Postgres for the database

Family Hub uses Postgres (its own container on the Pi) rather than SQLite, trading the operational simplicity of a single-file database for more headroom and standard tooling as the app grows past a handful of trackers.
